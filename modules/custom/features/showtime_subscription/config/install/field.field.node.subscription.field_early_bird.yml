langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_early_bird
    - node.type.subscription
id: node.subscription.field_early_bird
field_name: field_early_bird
entity_type: node
bundle: subscription
label: 'Early Bird'
description: 'Check if this purchase is before the early bird deadline.'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'Yes'
  off_label: 'No'
field_type: boolean
